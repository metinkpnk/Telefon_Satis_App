<Window x:Class="TelefonSatısApp.TelefonEkle"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Telefon Ekle" Height="780" Width="520"
        WindowStartupLocation="CenterScreen"
        Background="#0F172A" WindowStyle="None" ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <!-- Converter -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Sistem vurgularını koyu temaya çek -->
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#2563EB"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#0F172A"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#E5E7EB"/>
        <!-- Palet -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#1E40AF"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#0B1221"/>
        <SolidColorBrush x:Key="CardBrush" Color="#111827"/>
        <SolidColorBrush x:Key="TextLight" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="TextMuted" Color="#94A3B8"/>

        <!-- Genel buton -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="46"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="12" ShadowDepth="0" Opacity="0.25"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#172554"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Giriş alanı -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="BorderBrush" Value="#1E293B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,6,0,14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ComboBox öğe stili: hover/seçim renkleri belirgin -->
        <Style x:Key="ComboItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Padding" Value="10,6"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1E293B"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ComboBox -->
        <Style TargetType="ComboBox">
            <Style.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#0F172A"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#0F172A"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#E5E7EB"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#E5E7EB"/>
            </Style.Resources>
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="BorderBrush" Value="#1E293B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,6,0,14"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ComboItemStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Ana border -->
                            <Border Grid.ColumnSpan="2"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"/>
                            
                            <!-- Editable TextBox -->
                            <TextBox x:Name="PART_EditableTextBox"
                                     Grid.Column="0"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="{StaticResource TextLight}"
                                     Padding="8,0"
                                     VerticalAlignment="Center"
                                     FontSize="{TemplateBinding FontSize}"
                                     Visibility="{Binding IsEditable, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            
                            <!-- Non-editable content -->
                            <ContentPresenter x:Name="ContentSite"
                                              Grid.Column="0"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Margin="8,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              IsHitTestVisible="False"/>
                            
                            <!-- Dropdown button -->
                            <ToggleButton x:Name="ToggleButton"
                                          Grid.Column="1"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="False"
                                          ClickMode="Press"
                                          Background="Transparent"
                                          BorderThickness="0">
                                <Path Fill="{StaticResource TextLight}"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Data="M 0 0 L 4 4 L 8 0 Z"/>
                            </ToggleButton>
                            
                            <!-- Popup -->
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   StaysOpen="False">
                                <Border Background="#0F172A"
                                        BorderBrush="#1E293B"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="4"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="90"/>
        </Grid.RowDefinitions>

        <!-- Üst bar -->
        <Border Grid.Row="0" Background="#0B1221">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0">
                    <Ellipse Width="10" Height="10" Fill="{StaticResource PrimaryBrush}" Margin="0,0,10,0"/>
                    <TextBlock Text="Telefon Ekle"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextLight}"/>
                </StackPanel>
                <Button Grid.Column="1"
                        Content="✕"
                        Width="34"
                        Height="34"
                        Background="Transparent"
                        Foreground="#F87171"
                        BorderThickness="0"
                        Click="CloseButton_Click"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Form kartı -->
        <Border Grid.Row="1"
                Margin="18,12,18,12"
                Padding="24"
                Background="{StaticResource CardBrush}"
                CornerRadius="20">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="16" ShadowDepth="0" Opacity="0.35"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal"
                            Grid.Row="0"
                            Margin="0,0,0,18"
                            VerticalAlignment="Center">
                    <StackPanel>
                        <TextBlock Text="Yeni Telefon"
                                   Foreground="{StaticResource TextLight}"
                                   FontSize="22"
                                   FontWeight="Bold"/>
                        <TextBlock Text="Zorunlu alanlar * ile belirtilmiştir."
                                   Foreground="{StaticResource TextMuted}"
                                   FontSize="12"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </StackPanel>

                <Grid Grid.Row="1"
                      Margin="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.05*"/>
                        <ColumnDefinition Width="0.95*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Sol kolon -->
                    <StackPanel Grid.Column="0" Margin="0,0,24,0">
                        <TextBlock Text="IMEI *" Foreground="{StaticResource TextMuted}" Margin="0,0,0,4"/>
                        <TextBox x:Name="TxtImei"/>

                        <TextBlock Text="Marka *" Foreground="{StaticResource TextMuted}" Margin="0,10,0,4"/>
                        <ComboBox x:Name="CbMarka"
                                  SelectionChanged="CbMarka_SelectionChanged"
                                  IsEditable="True"/>

                        <TextBlock Text="Model *" Foreground="{StaticResource TextMuted}" Margin="0,10,0,4"/>
                        <ComboBox x:Name="CbModel"
                                  IsEditable="True"/>

                        <TextBlock Text="Renk" Foreground="{StaticResource TextMuted}" Margin="0,10,0,4"/>
                        <ComboBox x:Name="CbRenk" IsEditable="True">
                            <ComboBoxItem Content="Açık Mavi"/>
                            <ComboBoxItem Content="Açık Sarı"/>
                            <ComboBoxItem Content="Açık Yeşil"/>
                            <ComboBoxItem Content="Altın"/>
                            <ComboBoxItem Content="Beyaz"/>
                            <ComboBoxItem Content="Bordo"/>
                            <ComboBoxItem Content="Gri / Gümüş"/>
                            <ComboBoxItem Content="Gümüş"/>
                            <ComboBoxItem Content="Kahverengi"/>
                            <ComboBoxItem Content="Kırmızı"/>
                            <ComboBoxItem Content="Mavi"/>
                            <ComboBoxItem Content="Mor / Lavanta"/>
                            <ComboBoxItem Content="Pembe"/>
                            <ComboBoxItem Content="Sarı"/>
                            <ComboBoxItem Content="Siyah"/>
                            <ComboBoxItem Content="Turuncu"/>
                            <ComboBoxItem Content="Turuncu / Kozmik Turuncu"/>
                            <ComboBoxItem Content="Yeşil"/>
                            <ComboBoxItem Content="Zeytin Yeşili"/>

                        </ComboBox>

                        <TextBlock Text="Durum" Foreground="{StaticResource TextMuted}" Margin="0,10,0,4"/>
                        <ComboBox x:Name="CbDurum" IsEditable="True">
                            <ComboBoxItem Content="Sıfır"/>
                            <ComboBoxItem Content="İkinci El"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Sağ kolon -->
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Garanti (Ay, 0–24)" Foreground="{StaticResource TextMuted}" Margin="0,0,0,4"/>
                        <ComboBox x:Name="CbGarantiAy"/>

                        <TextBlock Text="Çıkış Yılı" Foreground="{StaticResource TextMuted}" Margin="0,10,0,4"/>
                        <ComboBox x:Name="CbCikisYili" IsEditable="True"/>

                        <TextBlock Text="Alınan Fiyat" Foreground="{StaticResource TextMuted}" Margin="0,10,0,4"/>
                <TextBox x:Name="TxtAlinanFiyat" TextChanged="TxtAlinanFiyat_TextChanged"/>

                        <Border Margin="0,16,0,0"
                                Padding="16"
                                Background="#0B1221"
                                CornerRadius="12">
                            <StackPanel>
                                <TextBlock Text="İpucu"
                                           Foreground="{StaticResource TextMuted}"
                                           FontWeight="SemiBold"/>
                                <TextBlock Text="Marka ve model seçiminden sonra fiyat ve durum alanlarını doldurduğunuzdan emin olun."
                                           TextWrapping="Wrap"
                                           Foreground="{StaticResource TextLight}"
                                           Opacity="0.8"
                                           Margin="0,4,0,0"
                                           FontSize="12"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- Alt buton -->
        <Border Grid.Row="2"
                Margin="18,0,18,10"
                Background="{StaticResource SurfaceBrush}"
                CornerRadius="14"
                Padding="12">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="12" ShadowDepth="0" Opacity="0.25"/>
            </Border.Effect>
            <Button Content="Kaydet" Click="Kaydet_Click" Height="54" VerticalAlignment="Top" RenderTransformOrigin="0.498,1.057"/>
        </Border>
    </Grid>
</Window>

