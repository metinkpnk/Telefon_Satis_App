<Window x:Class="TelefonSatısApp.SilOnayPenceresi"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Onay"
        Height="280"
        Width="420"
        Background="#0F172A"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#1E40AF"/>
        <SolidColorBrush x:Key="CardBrush" Color="#111827"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#0B1221"/>
        <SolidColorBrush x:Key="TextLight" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="TextMuted" Color="#94A3B8"/>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="12" ShadowDepth="0" Opacity="0.25"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#172554"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Margin="16"
            Padding="18"
            Background="{StaticResource CardBrush}"
            CornerRadius="16">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="16" ShadowDepth="0" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="40"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="10" Height="10" Fill="{StaticResource PrimaryBrush}" Margin="0,0,10,0"/>
                    <TextBlock x:Name="TxtTitle"
                               Text="Onay"
                               Foreground="{StaticResource TextLight}"
                               FontSize="16"
                               FontWeight="Bold"/>
                </StackPanel>
                <Button Grid.Column="1"
                        Content="✕"
                        Background="Transparent"
                        Foreground="#F87171"
                        BorderThickness="0"
                        Width="32"
                        Height="32"
                        Click="Cancel_Click"/>
            </Grid>

            <StackPanel Grid.Row="1" Margin="0,20,0,10">
                <TextBlock x:Name="TxtMessage"
                           Text="Bu işlem onaylanacak mı?"
                           TextWrapping="Wrap"
                           Foreground="{StaticResource TextLight}"
                           FontSize="14"/>
                <TextBlock Text="Bu işlem geri alınamaz."
                           Margin="0,8,0,0"
                           Foreground="{StaticResource TextMuted}"
                           FontSize="12"/>
            </StackPanel>

            <Grid Grid.Row="2" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Content="Vazgeç"
                        Grid.Column="0"
                        Background="#1E293B"
                        Foreground="{StaticResource TextLight}"
                        Click="Cancel_Click"
                        Margin="0,0,8,0"/>
                <Button Content="Evet, Sil"
                        Grid.Column="1"
                        Click="Confirm_Click"
                        Margin="8,0,0,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>

